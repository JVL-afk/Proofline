# Local Platform Boundary

Reserved for the reproducible local development platform after its ADR is accepted.

The architecture anticipates PostgreSQL, Redis (when required), S3-compatible storage, and a Temporal development environment. Exact services, versions, ports, persistence, health checks, and credentials must be pinned and documented; no floating `latest` images are permitted.
