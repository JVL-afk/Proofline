# Audit Local Adapters

Development-only M3 adapters: SQLite persistence and read-only catalogs over the canonical local
M1/M2 repositories. They do not duplicate raw source bodies, call AI providers, publish artifacts,
or perform external actions. Production persistence, workflow, identity, and retention remain open.
