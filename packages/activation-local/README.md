# Activation Local

M6.5 local persistence stores immutable governance/readiness records in SQLite. It has no network,
provider, cloud, identity, domain, secret, contact-source, verification, or delivery adapter.

Only repository-owned configuration and synthetic test records are permitted locally.
