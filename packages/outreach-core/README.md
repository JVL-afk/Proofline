# Outreach Core

Owns M5 immutable outreach manifests, claim projections, deterministic plain-text artifacts, hard
QC, and content-only review. It consumes exact approved M1-M4 contracts through read-only ports.
It never discovers recipients, exports or sends content, invokes live AI, or performs an external
side effect. `CONTENT_APPROVED` is wording approval only.
