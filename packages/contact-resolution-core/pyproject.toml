[project]
name = "opintel-contact-resolution-core"
version = "0.1.0"
description = "M6.10 deterministic recipient & channel resolution engine"
requires-python = ">=3.13,<3.14"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/opintel_contact_resolution"]
