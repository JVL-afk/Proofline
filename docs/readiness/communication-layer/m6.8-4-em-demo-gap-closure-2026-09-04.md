# M6.8-4 addendum — E+M demo evaluation (gap closure, 2026-09-04)

Owner authorization 2026-09-04 "CLOSE E+M M4 EVALUATION GAP, THEN UPDATE M6.9
DRAFT". M6.8-4 evaluated A-Plus and Elite as **communication + demo**; E+M was
correctly gated at the **communication** layer as
`COMMUNICATION_NOT_DISTINCTIVE_ENOUGH`, but its **demo** potential was not run
through the frozen presentation compiler. This addendum closes that gap.

**The E+M communication result is unchanged and immutable:
`NOT_DISTINCTIVE_ENOUGH`.** No A-Plus or Elite result changed. No provider call.
**USD 0** — this is a pure deterministic M4 evaluation.

## 1. Exact frozen E+M source artifact identities

| | |
|---|---|
| fixture | `tests/m68_fixtures.em_envelope()` |
| envelope sha256 | `2c35afc05382f3f26e3cf279e0736fdb09caa9145656420f636510ea218eaf98` |
| m2–m5 bundle sha256 | `a6540437f096ac7b6d0e026fd75ebdffbd5a92a1b77f59e730fb3ee24b3554e7` |
| business identity | E+M Emergency Air Conditioning / `www.emergencyac.org` (display name + hostname only) |
| M4 compiler | `demo.builder_brief@3.2` (FROZEN) — the exact compiler used for A-Plus and Elite |

No browsing, no re-research, no website refresh, no added facts, no inferred
geography / current process / response behaviour / integrations / business
problems.

## 2. Was a builder brief generated?

**Yes.** The frozen M4 pipeline did **not** gate E+M before builder-brief
creation. `compile_builder_story(envelope=em_envelope())` runs cleanly and
returns a full `BuilderStory`. The V1 `compile_builder_brief` mode logic assigns
it deterministically:

- `has_intake_surface = True` (`"Request Service"`), `len(evidence) = 3`,
  `has_commercial = True` → **not** `DEMO_NOT_DISTINCTIVE_ENOUGH`;
- no strong anchor (no response-commitment / availability fact; commercial and
  intake facts are both `generic_label = True`), 0 non-generic facts,
  `has_distinctive_fact() = False` → **not** `BESPOKE_DEMO`, **not**
  `EVIDENCE_BOUND_DEMO`;
- → **`GENERIC_CAPABILITY_DEMO`**.

Artifacts: `docs/readiness/demo-builder-brief/m4-em-builder-brief-v3.2-2026-09-04.{json,md}`.

## 3. E+M demo disposition

### `GENERIC_CAPABILITY_DEMO`

| field | value |
|---|---|
| **primary anchor** | `commercial context` — the weakest-tier, **generic** category label (`"Commercial AC"`). Not a company-specific anchor. |
| **opportunity headline** | *"One way a **generic** structured intake flow could organize incoming commercial HVAC requests"* |
| **opportunity summary** | *"Your public pages show **broad commercial-HVAC service labels only**. We built a short simulation of a **generic** structured intake flow that prepares a commercial request for human follow-up — **not built around your business**. We don't know your current internal process, systems, staffing, or response performance."* |
| **scenario framing** | the default (non-anchor-specific): *"a commercial request, organized so a person can pick it up"* |
| **"Why we built this" evidence detail** | **empty** — `_known_public_context` drops all `generic_label` facts, so the customer view shows **no quoted evidence bullets at all** |
| **evidence used** | the three generic labels justify only that a commercial-intake *field set* exists — `"Request Service"` / `"Commercial AC"` / `"Service Areas"` appear only in the audit layer (`m4_option_labels`), never as customer copy or form values |
| **evidence omitted** | none available to omit — there is no company-specific evidence |
| **UNKNOWNs preserved** | all 8, in the underlying brief: RESPONSE_PERFORMANCE, DEMAND_VOLUME, CONVERSION, CUSTOMER_VALUE, CURRENT_PROCESS, COST, FEASIBILITY, INTEGRATION |
| **V3.2 customer-experience rules** | applied identically — synthetic demo inputs, fictional handoff (Morgan / Intake coordinator), 3 grouped outcomes (all 6 mock actions preserved), 4 example-scenario presets from authorized synthetic values only, `Start over` as local reset, persistent *"Simulation · not connected to E+M"*, no ontology leakage, no builder-invented functionality, no real contact data, no real action |

Shared mechanics (Screen 2 fields, synthetic values, result stages, outcome
groups, closing bridge) are **identical** to A-Plus and Elite — that is
authorized (shared intake engine, different reason/story). **No new workflow was
invented for E+M.**

## 4. Hidden-name personalization test

> If the company name were hidden, would the resulting demo still contain enough
> evidence-shaped context to tell a reviewer this was built from E+M-specific
> evidence rather than a generic commercial-HVAC intake template?

**No.** With `E+M` / `emergencyac.org` / the fictional name removed, the demo
contains:

- a headline that literally says **"generic"**;
- a summary that says **"broad commercial-HVAC service labels only … not built
  around your business"**;
- **zero** "Why we built this" evidence bullets;
- otherwise only the shared template mechanics.

There is no evidence-shaped context in the customer view. A reviewer could not
distinguish it from a generic commercial-HVAC intake template — **which is
correct**, because E+M's frozen evidence is genuinely generic. **The hidden-name
test fails, as the rules require.** Disposition stays `GENERIC_CAPABILITY_DEMO`
(not forced up to `EVIDENCE_BOUND_DEMO` / `BESPOKE_DEMO`; not forced down to
`DEMO_NOT_DISTINCTIVE_ENOUGH` — E+M does have enough for a truthful generic
commercial-intake simulation, it just has nothing company-specific).

## 5. Complete three-company matrix

| company | communication (M6.8-4, unchanged) | demo (frozen `demo.builder_brief@3.2`) | combined interaction |
|---|---|---|---|
| **A-Plus** | `SHADOW_READY_FOR_LAUNCH_REVIEW` — ACCEPTABLE 0.86 ×4, uses strongest hook, company-specific, 0 safety violations | **BESPOKE_DEMO** — anchor `response commitment`; headline "…support the response experience your public pages already describe"; distinctive | **the one package worth launch review** — safe, personalized at both layers, communication ↔ demo coherent; needs disclosure-slot resolution + the M6.8-3 Path A/B decision (not send-ready as-is) |
| **Elite** | `SHADOW_SAFE_BUT_WEAK` — truth intact, coverage 1.0 on the best candidate, no availability→response upgrade; but `NOT_DISTINCTIVE` 3/4 generations | **BESPOKE_DEMO** — anchor `service availability`; headline "…complement the service availability your public pages already describe"; distinctive (availability as context only) | **not a launch candidate** — the demo personalizes but the communication does not; the package's distinctiveness is carried by the demo alone |
| **E+M** | `NOT_DISTINCTIVE_ENOUGH` — deterministic gate, 0 generations | **GENERIC_CAPABILITY_DEMO** — anchor `commercial context` (generic); headline explicitly "generic"; **no evidence-shaped customer context**; hidden-name test fails | **not a launch candidate** — non-distinctive at **both** layers. The generic demo is truthful and usable but does **not** upgrade the non-distinctive communication package. |

**For E+M, explicitly:**

- **communication weakness:** the transformer's deterministic distinctiveness
  gate declined to generate — E+M's evidence has no fact distinctive enough for
  a non-generic opening.
- **demo distinctiveness:** none. The demo is a *generic capability*
  demonstration; it disclaims being built around E+M; it contains no quoted
  evidence.
- **worth launch review?** **No.** A merely-usable generic demo does not upgrade
  a non-distinctive communication package into a launch candidate.

## 6. Updated overall M6.8-4 conclusion

On real frozen evidence the accepted `claude-sonnet-5` transformer produced 8
candidates with **0 genuine provider safety violations**; the frozen
`demo.builder_brief@3.2` compiler produced a demo for all three companies with
**no `opintel_demo` change and USD 0**.

**Exactly one company — A-Plus — has a communication + demo package that is
distinctive and coherent at both layers, and is therefore the single candidate
for a bounded, human-reviewed first-contact test** (subject to the owner's
Path A / Path B decision on the M6.8-3 formal blocker and human resolution of the
disclosure slots).

- **Elite** personalizes at the demo layer only; its communication is
  `SHADOW_SAFE_BUT_WEAK`. Not a launch candidate.
- **E+M** is non-distinctive at **both** layers (`NOT_DISTINCTIVE_ENOUGH`
  communication + `GENERIC_CAPABILITY_DEMO`). Not a launch candidate. This is a
  **success of the truth system** — it declined to manufacture personalization
  from generic evidence, at both the communication and the demo layer.

`PERSONALIZATION_PROOF` result: **`PASSED`** for A-Plus vs Elite at the demo
layer + on quoted evidence; **not passed** for E+M (correctly — generic
evidence). The three-company matrix is now complete.

## 7. Confirmations

- **No A-Plus or Elite result was changed.** A-Plus stays
  `SHADOW_READY_FOR_LAUNCH_REVIEW`; Elite stays `SHADOW_SAFE_BUT_WEAK`; both
  demos stay `BESPOKE_DEMO`; the A-Plus and Elite `.md` artifacts are
  byte-identical to their committed versions.
- **The E+M communication result is unchanged:** `NOT_DISTINCTIVE_ENOUGH`.
- **M6.8-3 stays `NOT_CERTIFIED`.** M4 stays frozen at `demo.builder_brief@3.2`.
- **No contact / send / delivery / browsing / policy change / signature / GO.**
- **No provider call.** The frozen M4 pipeline is fully deterministic; a provider
  call would not have been required and none was made.

Test: `tests/test_m4_builder_brief_v3.py::test_em_frozen_compiler_produces_a_generic_capability_demo`.
