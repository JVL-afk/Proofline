# M6.9 Stage B — real bounded A-Plus engine run report

Owner authorization 2026-09-05 "IMPLEMENT SUPPRESSION / OPT-OUT CONTROLS, THEN
EXECUTE REAL BOUNDED A-PLUS ENGINE RUN", Stage B. Runs only because Stage A
(`SUPPRESSION_OPT_OUT_CONTROLS = READY_FOR_BOUNDED_PILOT`, commit `37ad4bb`)
is green.

## 0. Scope, stated honestly

This produced a genuine real evidence capture and a genuine single real
Sonnet-5 generation — it did **not** stand up a full DB-backed workspace
running the M1 crawl worker / M2 opportunity engine / M3 audit engine end to
end (that requires production crawler/worker infrastructure this bounded
task does not provision). Company-specific facts, their exact source URLs,
and their content hashes below are real, freshly captured, and verified
against the raw HTML (not the `tests/m68_fixtures.py` fixture's placeholder
`https://example.com/...` + `"a" * 64` provenance). Structural lineage IDs
(`workspace_id`, `business_id`, `research_run_id`, etc.) are freshly generated
UUIDs standing in for what a real DB-backed pipeline run would assign, since
no such run executed; they are not stable across repeated invocations of
`scripts/run_m69_real_bounded_aplus.py`, only the bound values in this report
are authoritative.

## 1. Real evidence captured (2026-09-05)

Via `curl` (real HTTP 200 each) against `www.aplusac.com`, verified with an
exact substring match against the raw HTML, sha256 computed over the raw
bytes:

| fact | quoted phrase | URL | content sha256 |
|---|---|---|---|
| INTAKE_SURFACE | "When to Schedule AC Replacement" | https://www.aplusac.com/press-releases/when-to-schedule-ac-replacement/ | `7a9934dcc38ffeae61a656a0533e1d24ec402f2661470610eb13964f048e6ad3` |
| COMMERCIAL_CONTEXT | "The Austin Commercial AC Repair Company" (H1) | https://www.aplusac.com/commercial-hvac/ | `38abd278346387e88c9e89e609c9f679d25c59d2eb48f90cdaf155b19604eef3` |
| RESPONSE_COMMITMENT | "we will do everything in our power to fix your problem fast, sometimes even on the same day" | https://www.aplusac.com/commercial-hvac/ | `38abd278346387e88c9e89e609c9f679d25c59d2eb48f90cdaf155b19604eef3` |
| SERVICE_AREA_CONTEXT | "Service Area" (H1) | https://www.aplusac.com/service-area/ | `d6ecb8709c821986b7da76f5893f6830a88ae71a3f765b01debb5abcfe85f8f3` |

The intake-surface and service-area phrases are byte-identical to what the
M6.8-4 shadow's frozen fixture used, confirming those two facts are still
true. The commercial-context phrase differs from the fixture's — see §4.

## 2. Real run identity

- Envelope sha256 (this exact run): `fee70396444b23074511ca496f6ec2904affbe738c79a77f1fe1e5fc1000b31d`
- M2-M5 bundle sha256 (unchanged, reused): `a6540437f096ac7b6d0e026fd75ebdffbd5a92a1b77f59e730fb3ee24b3554e7`
- Distinctiveness gate: **PASSED** (`has_distinctive_fact() == True`)
- Script: `scripts/run_m69_real_bounded_aplus.py --execute`
- Report JSON: `docs/readiness/communication-layer/m6.9-real-run-aplus-2026-09-05.json`

## 3. M2/M3 result (as constructed for this real run)

No new opportunity-hypothesis or audit-engine DB run executed (§0). The
opportunity disposition carried forward is the same one already established
for A-Plus across M6.8-3/M6.8-4: commercial-HVAC inbound intake /
qualification, with `RESPONSE_COMMITMENT` as the strongest licensed anchor
(a business self-claim, not a verified fact — `RESPONSE_PERFORMANCE` stays an
explicit `UNKNOWN`, unchanged). Economics remain `insufficient_data`
(unchanged — no real economics input exists for this business).

## 4. M4 disposition and personalization

Compiled the real envelope through the frozen, unmodified
`demo.builder_brief@3.2` compiler (no M4 code change).

- M4 builder-brief sha256: `533f586a788d78441186f021c5120488f1fa156167a9c239d537851da0b7979a`
- Output: `docs/readiness/demo-builder-brief/m6.9-real-run-aplus-builder-brief-2026-09-05.md`
- Disposition: `BESPOKE_DEMO`, anchor = response commitment (same anchor
  category the earlier shadow selected)
- Personalization: unchanged from the earlier A-Plus vs Elite proof — this
  real run only re-confirms A-Plus's own bespoke demo continues to compile
  cleanly from genuinely current evidence.

## 5. Communication artifact — real single-candidate generation

One real `claude-sonnet-5` call (prompt `@9`, thinking disabled, contract v3)
— the single bounded pilot candidate, not the shadow's N=3 pattern.

- Cost: **USD 0.032562** (well inside any reasonable ceiling)
- Served model: `claude-sonnet-5`, stop_reason `end_turn`, 6796 in / 1897 out
  tokens, 15.0s latency
- Subject (frozen): **"A quick comparison idea for A-Plus AC's intake"**
  — `subject_has_adv_prefix: false` (DO_NOT_PREPEND_ADV honored)
- Body (frozen, `sha256 7e00ba671aea0a488693bf44587e689c1f73b26db23be45833bdd6646532aaec`):

  > While reviewing A-Plus Air Conditioning & Home Solutions' public pages, I
  > noticed you describe commercial HVAC work ("The Austin Commercial AC
  > Repair Company") and publish guidance like "When to Schedule AC
  > Replacement." This isn't a claim about how your team works today — we
  > have no visibility into that. Based only on public information, a short
  > deterministic simulation was prepared using synthetic example inputs, not
  > real customer data, and it routes every case to a human review step; it
  > is a simulation, not a system deployed, connected, official, or operated
  > by your business. A step that acknowledges and sorts new service requests
  > could be evaluated. Would you be open to comparing this simulation with
  > your actual intake process?
  > {{verified_sender_signature}}
  > {{required_postal_disclosure}}
  > {{approved_opt_out_instruction}}
  > {{functional_role_or_team}}

- With the known Proofline/Andrew disclosure slots resolved
  (`opintel_suppression.resolve_known_disclosure_slots`):

  > ...Would you be open to comparing this simulation with your actual intake
  > process?
  > Andrew
  > Proofline
  > andrew@proofline.business
  > Proofline, Str. Lucian Blaga, nr. 8, Ciugud, Alba 517240, Romania
  > To stop receiving email from Proofline, reply to this message with the
  > word UNSUBSCRIBE. We will remove your address within three days.
  > the Proofline team

- **No `{{exact_demo_url}}` token exists in this candidate** — the prompt
  template used for this generation never asked for one (ADR-0079's
  demo-link insertion is a separate, later manual step, not exercised here).
  The demo URL therefore remains the single owner-supplied field regardless
  of this run's outcome, exactly as the prior pilot package already stated.

## 6. Safety / validation result — reality wins

**Validator: `passed = False`.** Findings: `unsupported_geography`,
`no_company_specific_evidence`. Canonical coverage 1.0; 0 unresolved
substantive claims. Quality assessor: `ACCEPTABLE` (0.86),
`uses_company_specific_evidence = True`, `uses_strongest_hook = True`.

This is **not a provider safety violation** — envelope fidelity holds (no
fabricated claim, no injected instruction, correct simulation/UNKNOWN
disclosures, correct sender/postal/opt-out slot handling). It is a genuine
deterministic-validator finding, and it traces to a real, honest cause: the
live A-Plus commercial-HVAC page's actual H1 is **"The Austin Commercial AC
Repair Company"** — it names a place ("Austin") inside the exact phrase this
architecture licenses Claude to quote verbatim for `COMMERCIAL_CONTEXT`. The
earlier M6.8-4 shadow's fixture used a place-name-free synthetic substitute
("Commercial Air Conditioning Repairs") for the same fact, which is why that
shadow never tripped this check. **Reality disagrees with the shadow
assumption here, and this report does not paper over it**: the real, live
evidence for A-Plus produces a communication candidate that a validator-clean
send-ready package requires reworking (either a different quotable
`COMMERCIAL_CONTEXT` phrase, or a validator/reconciler change to allow a place
name inside a directly-quoted business phrase — both out of this bounded
task's scope; the second option is exactly the kind of "reconciler
gate-fitting" this project has repeatedly ruled out doing casually). The
`no_company_specific_evidence` finding is a related deterministic-reconciler
signal, not independently investigated further here (out of scope; consistent
with the already-owner-acknowledged M6.8-3 `NOT_CERTIFIED` reconciler
completeness limitation).

No second call was made to try for a passing result — that would be exactly
the "retry until it passes" behavior this project has repeatedly rejected.
One real call, reported honestly, is the bounded pilot this authorization
asked for.

## 7. UNKNOWN preservation

`RESPONSE_PERFORMANCE`, `DEMAND_VOLUME`, `CONVERSION`, `CUSTOMER_VALUE`,
`CURRENT_PROCESS`, `COST`, `FEASIBILITY`, `INTEGRATION` — all preserved,
none assumed, none resolved by this run.

## 8. Suppression-system readiness

`SUPPRESSION_OPT_OUT_CONTROLS = READY_FOR_BOUNDED_PILOT` (Stage A, §9 of
`m6.9-suppression-opt-out-controls-2026-09-05.md`). Not exercised by this
run — no recipient exists to check against it yet.

## 9. Divergence from the frozen M6.8-4 shadow assumption

The shadow's A-Plus disposition was `SHADOW_READY_FOR_LAUNCH_REVIEW`. This
real run, using genuinely current live evidence and a single real generation,
produces a communication candidate that does **not** pass the deterministic
validator, for the structural reason in §6. **This is a real, materially
different result, and it stands as reported — it is not forced to match the
earlier shadow's outcome.**

## 10. Final status

**`REAL_A_PLUS_RUN_COMPLETE`** — the real bounded run executed successfully
end to end (real evidence capture, real envelope, real single Sonnet-5 call,
real M4 compile, real disclosure-slot resolution) and produced a truthful
result. It is **not** the same as a validator-clean, send-ready package: this
run's own communication candidate needs the §6 rework before it could be
combined with the still-unresolved fields the M6.9 pilot package already
listed (demo URL, human reviewer, legal/A-17 clearance, and now also a
validator-clean candidate). Nothing here changes M6.9's bounded-GO decision,
M6.8-3's `NOT_CERTIFIED` status, or Path B's scope. No contact was resolved.
No email was sent. No demo was deployed.
