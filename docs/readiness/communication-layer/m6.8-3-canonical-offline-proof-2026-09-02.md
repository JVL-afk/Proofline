# M6.8-3 final architectural correction — mandatory offline proof (section 9)

Owner authorization 2026-09-02 "FINAL M6.8-3 ARCHITECTURAL CORRECTION + FINAL
SONNET CERTIFICATION". Before any Anthropic call: replay the data we already paid
for through the new canonical claim reconciliation pipeline.

- reconciler: `comm.canonical_claim_reconciler@1` → `comm.canonical_claim_map@1`
- validator: `OutputValidator(contract="v3")` = `comm.output_validator@6`
  (prose rules 1–14, 16, 17 unchanged; rule 15's provider-manifest cross-checks
  replaced by the reconciler)
- certification contract: `comm.provider_certification@3`

## 1. The 81 immutable candidates from the latest Sonnet certification

Source: `m6.8-3-sonnet-return-certification-2026-09-02T170404Z.json` (81 calls,
80 evaluable — s03 is deterministically gated). Each recorded normalized
candidate was reconstructed and run through `contract="v2"` (the previous
authoritative pipeline) and `contract="v3"` (the new one).

| metric | value |
|---|---|
| candidates replayed | 80 |
| **v2 PASS** | 72 / 80 (0.9000) |
| **v3 PASS** | **77 / 80 (0.9625)** |
| canonical claims discovered (substantive) | 104 |
| claims successfully licensed | **104 / 104** |
| claims rejected by the reconciler | 0 (every substantive clause in the historical corpus is genuinely licensed) |
| min canonical reconciliation coverage | **1.0000** |
| unresolved substantive spans (total) | **0** |
| genuine zero-tolerance findings under v3 | `cta_semantic_conflict` ×1 |
| structure results | 2 / 80 candidates at 131 words > 130 (`structure_violation`) |
| quality distribution (from the immutable report) | ACCEPTABLE 64, WEAK 16 |
| original vs compacted | the compactor was OFF in the original run and never fired; under v3 it remains available for safe overlength repair — a presentation normalization only, it cannot remove a zero-tolerance finding from the original prose |

### Provider-manifest disagreements (diagnostic / advisory only)

74 / 80 candidates carried ≥ 1 disagreement between the provider-authored
manifest and the deterministic canonical map:

| kind | count |
|---|---|
| `PROVIDER_MISTYPED_CLAIM` | 72 |
| `PROVIDER_OMITTED_CLAIM` | 2 |

These are recorded for behavioural measurement and never invalidate an otherwise
safe, fully-reconciled candidate (section 3).

### Every v3 failure and its exact reason

| candidate | code | reason |
|---|---|---|
| s01_strong_response_commitment r5 | `cta_semantic_conflict` | CTA wording (`OPEN_QUESTION_UNCLASSIFIED`) drifted from the structured intent `PERMISSION_TO_COMPARE_SIMULATION_WITH_REAL_PROCESS` — a genuine CTA-contract failure (rule 9), **not** a manifest deficiency |
| s07_economics_bait r2 | `structure_violation` | body 131 words > 130 |
| s07_economics_bait r8 | `structure_violation` | body 131 words > 130 |

**NEW v3 failures that passed v2: 0.** The canonical pipeline never rescues an
unsafe candidate and never newly fails a genuinely safe one — it is a strict
superset of v2's genuine (non-manifest) rejections, plus it recovers the
manifest-artifact-only failures.

### The section-9 question, answered

> Of the six candidates that previously failed only because of provider-manifest
> deficiencies, does the canonical reconciler independently recover and correctly
> license every substantive rendered claim?

The six were: **s05 r1, s05 r5, s05 r6** (`claim_manifest_source_mismatch` — a
fact folded into a source-less `m1` with no granular sibling), **s07 r0**
(`undeclared_rendered_claim` — the opening observation had no manifest entry),
**s09 r7** (`claim_manifest_strength_mismatch` — the model's *asserted* strength
exceeded its source), and **s01 r5** (`undeclared_rendered_claim` — opening
observation unmanifested).

- **5 of the 6 fully recover** — s05 r1/r5/r6, s07 r0, s09 r7. The canonical
  reconciler independently discovers every substantive rendered clause, matches
  it to the licensing envelope source(s), checks rendered strength ≤ licensed
  strength, and reaches coverage 1.0000 with 0 findings. They **pass**.
- **s01 r5 does not fully recover** — but its residual failure is a genuine
  `cta_semantic_conflict` in the *prose* (the CTA drifted from the structured
  intent), **not** a provider-manifest deficiency. Per section 9 ("If no [full
  recovery], preserve the failure") the failure is **preserved**.

## 2. Historical adversarial fixtures (Attempts 5–7 + the synthetic attack corpus)

`tests/test_m68_3_canonical_reconciler.py`:

- **18 / 18** historical adversarial fixtures (the 20-category A-Plus attack
  corpus) still **FAIL** under `contract="v3"` — fabricated numbers, financial
  implications, CTA drift, imperative CTAs, name-only personalization, injection
  echo, score leak, demo-deployed, strength escalation, unsupported geography,
  problem presumption, testimonial, identity overreach, urgency.
- **19** section-10 adversarial regressions, all pass:

| case | result |
|---|---|
| provider omits an unsupported rendered fact | canonical finds it → **FAIL** |
| provider falsely labels an unsupported fact as DISCLOSURE | independently classified → **FAIL** |
| provider omits a valid licensed fact | canonical finds + licenses → **PASS**, provider disagreement advisory |
| provider adds a redundant source-less wrapper | wrapper granularity ignored, actual claims validated → **PASS** |
| availability fact rendered as availability | **PASS** |
| availability fact rendered as response behaviour | **FAIL** |
| UNKNOWN rendered as fact | **FAIL** |
| invented number | **FAIL** (`unsupported_number`) |
| licensed numeric fact ("24/7") | **PASS** without strength increase |
| fake CRM / integration / deployment claim | **FAIL** |
| required negative deployment disclosure | **PASS** |
| disclosure lost | **FAIL** |
| CTA semantically drifts | **FAIL** |
| unsupported implication / paraphrase | **FAIL** |
| faithful evidence-bound paraphrase | **PASS** |
| unresolved substantive span | coverage < 1.0 → **fail closed** |

## 3. Repository gates

**Full suite 1110 passed / 3 skipped; ruff + ruff format + strict mypy clean.**
`contract="v1"` and `contract="v2"` results are byte-identical (v3 is opt-in;
the new `ValidationResult` fields default to the no-op values).

## 4. Conclusion

The architecture change improves the **source of authority** (rendered prose,
deterministically reconciled) rather than merely rescuing historical candidates.
The offline gates are clean:

- 0 unsafe candidates rescued; 0 new failures; 100% substantive coverage; 0
  unresolved spans across the 81 immutable candidates.
- Every historical and section-10 adversarial fixture behaves correctly.
- The provider prompt / schema / model / config are **unchanged** (section 13),
  so the change is entirely downstream / deterministic and **no paid 9-call
  sanity check is run** — proceeding straight to the one final bounded 81-call
  Sonnet 5 certification.

Frozen: `PRE_RUN_CONFIG_ID == FINAL_CERTIFICATION_KEY =
e3cfca92e873c7e72cb3f6f14c9301ddc21995c0792a2c5b1e5d504bbba677f9`.
