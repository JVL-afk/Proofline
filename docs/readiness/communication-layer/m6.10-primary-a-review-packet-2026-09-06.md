# PRIMARY_A review packet — A-Plus first contact (Greg Yamin)

Owner authorization 2026-09-06 "RECORD HUNTER REGISTRY APPROVAL AND RESUME
LIVE M6.10 GREG RESOLUTION". Machine-readable form:
`m6.10-aplus-primary-a-package-2026-09-06.json`.

**Nothing has been sent.** This packet is for PRIMARY_A's exact-package
review; Andrew makes the final send / do-not-send decision after that.

## Hunter registry approval — recorded

`TEXAS_DATA_BROKER_REGISTRY_CHECK = APPROVED_BY_COUNSEL`
`HUNTER_REGISTRY_DECISION = HUNTER_REGISTRY_APPROVED`

Recorded in `m6.10-hunter-vendor-record-2026-09-06.json` from PROJECT_OWNER's
transcription of Texas counsel's operational decision (2026-09-06),
specific to Hunter for the currently authorized M6.10 contact-enrichment
use. No registry entry number, legal entity name, registration date,
exemption rationale, or counsel identity metadata was invented. All
previously encoded `APPROVED_WITH_CONTROLS` requirements preserved.

**Vendor checklist: 8 of 8 satisfied → `HUNTER_PROVIDER_ACTIVATION = AUTHORIZED_WITH_CONTROLS`.**

## Hunter usage — bounded

| | |
|---|---|
| Email Finder calls | **1** |
| Email Verifier calls | **1** |
| Credits / cost | 1 request each; plan-dependent (not exposed in the report) |
| Key handling | read from `apikeys.txt` only, in-memory only, `del`-eted after each request URL, never logged / returned / hashed / committed |
| Bulk / lists / campaigns / sequences | none — not implemented in the adapter |
| Other providers | none activated (§15) |

## Recipient

| field | value |
|---|---|
| **Name** | Greg Yamin |
| **Role** | President |
| **Company** | A-Plus Air Conditioning & Home Solutions |
| **Work email** | `greg@aplusac.com` |
| **Identity provenance** | first-party — `aplusac.com/about-us/our-team/` ("Greg Yamin - President"); corroborated by the BBB business profile ("Mr. Gregory K. Yamin, President") |
| **Email provenance** | Hunter Email Finder, confidence **97**, provenance **OBSERVED** (Hunter reports the address seen on 8 public sources — not pattern-generated) |
| **Verification** | Hunter Email Verifier: `status=valid`, `mx_records=true`, `smtp_check=true`, **not catch-all**, `verifier_score=100` |
| **Verification method / date** | approved third-party verification service (Hunter Email Verifier), verified 2026-09-05, **fresh (<90 days)** |
| **Eligibility disposition** | `ELIGIBLE_VERIFIED_RECIPIENT` |

Provenance is OBSERVED, so the pattern-derived / one-candidate / INFERRED
rules did not need to apply. Corporate-domain match confirmed
(`aplusac.com` = the verified A-Plus domain); no personal / free-email
domain.

## From / Reply-To

```
From:     Andrew <andrew@proofline.business>
Reply-To: andrew@proofline.business
```

## Subject (exact, unchanged — validated real A-Plus communication)

```
A quick comparison idea for A-Plus AC's intake
```

No `ADV:` prefix (counsel's `DO_NOT_PREPEND_ADV` instruction).

## Email — exact fully rendered body (no unresolved tokens)

```
While reviewing A-Plus Air Conditioning & Home Solutions' public pages, I noticed you describe commercial HVAC work ("The Austin Commercial AC Repair Company") and publish guidance like "When to Schedule AC Replacement." This isn't a claim about how your team works today - we have no visibility into that. Based only on public information, a short deterministic simulation was prepared using synthetic example inputs, not real customer data, and it routes every case to a human review step; it is a simulation, not a system deployed, connected, official, or operated by your business. A step that acknowledges and sorts new service requests could be evaluated. Would you be open to comparing this simulation with your actual intake process?

The simulation: https://ac-request-demo.preview.emergentagent.com/

Andrew
Proofline
andrew@proofline.business
Proofline, Str. Lucian Blaga, nr. 8, Ciugud, Alba 517240, Romania
To stop receiving email from Proofline, reply to this message with the word UNSUBSCRIBE. We will remove your address within three days.
the Proofline team
```

**Reviewer note:** the trailing line `the Proofline team` is the resolved
`{{functional_role_or_team}}` disclosure slot (a generic team-level
descriptor derived from the company name — no personal title was invented).
PRIMARY_A may reposition or drop it as a non-substantive edit; any
substantive change re-triggers validation.

## Demo

`https://ac-request-demo.preview.emergentagent.com/` — the single ADR-0079
permitted URL, inserted verbatim, no tracking parameters, no redirect, no
token. It is the only URL in the message.

## Contact evidence chain

1. **Identity**: A-Plus's own team page + BBB business profile — first-party
   verified, `HIGH` confidence.
2. **Email endpoint**: Hunter Email Finder (confidence 97, OBSERVED on 8
   public sources) → Hunter Email Verifier (`valid`, MX true, not
   catch-all, score 100).
3. **Minimized durable fields persisted**: name, employer, role, work
   email, source (Hunter + first-party/BBB), finder confidence,
   verification method, verification result, verification date. Every
   other Hunter-returned field (including its mis-parsed `company: "Plus"`
   string and any raw source list beyond the count) was dropped before
   durable persistence.

## Final deterministic validation

Ran `OutputValidator(contract="v3")` on the frozen body (disclosure slots
still `{{...}}` — the state the validator's contract expects). **`passed: True`.**
Findings: `no_company_specific_evidence` (ADVISORY-only under v3 — a
personalization-quality flag, not a safety failure) and
`provider_manifest_disagreement` (ADVISORY — an artifact of reconstructing
the candidate with an empty claim manifest, since Stage B did not persist
the raw provider manifest). Canonical coverage 1.0; 0 unresolved
substantive claims. No `HARD_FAILURE` finding — the geography issue fixed
on 2026-09-05 (`LICENSED_SOURCE_PHRASE_WITH_GEOGRAPHY`) stays fixed.

Recipient-bound rendering checks (all pass): no unresolved tokens; exactly
one URL and it is the approved demo URL; postal disclosure present; opt-out
present; Reply-To present; no `ADV:` prefix; demo-status-truth language
present.

Exact-version bindings: envelope facts from the frozen `real_aplus_envelope`
(same real evidence: intake `sha256 7a9934dc…`, commercial `sha256 38abd278…`,
service-area `sha256 d6ecb870…`); communication body from
`m6.9-real-run-aplus-2026-09-05.json` (`body_frozen_sha256 7e00ba67…`);
M4 demo brief `demo.builder_brief@3.2` (per ADR-0079). Any substantive
mutation after this point invalidates the package.

## Suppression result

| check | result |
|---|---|
| Address suppression (`greg@aplusac.com`) | **clear** — pre-send gate returned `eligible` |
| Domain suppression (`aplusac.com`) | **clear** |
| Person suppression (Greg Yamin) | **clear** |
| Company do-not-contact (A-Plus) | **clear** |

No opt-out is on record. (These ran against a fresh in-memory registry; the
pilot has no persistent suppression store yet — a note, not a blocker,
since no opt-out has occurred.)

## Compliance

- **Postal disclosure**: Proofline, Str. Lucian Blaga, nr. 8, Ciugud, Alba
  517240, Romania — present in the body.
- **Opt-out**: "To stop receiving email from Proofline, reply to this
  message with the word UNSUBSCRIBE. We will remove your address within
  three days." — present; monitored mailbox `andrew@proofline.business`;
  3-day max SLA (`opintel_suppression`).
- **Path B exception**: ADR-0078 / ADR-0079 — one bounded A-Plus first
  contact, one recipient, one message, one URL, one occasion.
- **M6.8-3 status**: `NOT_CERTIFIED` — acknowledged, not waived.
- **Telemetry**: `HOST_PLATFORM_TELEMETRY_CONFIRMED_PRESENT` — the Emergent
  hosting template embeds PostHog session-recording + Cloudflare
  bot-management; not Proofline-added; ADR-0079's determination that host
  telemetry is not itself prohibited is unchanged. **The demo is not
  described as zero-tracking anywhere.**
- **After send**: **NONE AUTOMATIC** — no follow-up, no sequence, no CRM
  state, no tracking, regardless of outcome.

## Channel ranking (re-run after Hunter)

| channel | eligible | disposition | score |
|---|---|---|---|
| **EMAIL** | **Yes** | `ELIGIBLE_VERIFIED_RECIPIENT` | 125 |
| LINKEDIN | Yes | `ELIGIBLE_MANUAL_CHANNEL` | 85 |
| PHONE | No | `RESOLVED_BUT_EXECUTION_NOT_AUTHORIZED` | (hard blocker) |

**Recommended channel: EMAIL.** LinkedIn (manual-only) and phone (resolved,
execution unauthorized) are preserved as alternatives — neither is used by
this package.

## Reviewer status

```
FINAL_HUMAN_REVIEWER      = PRIMARY_A
FINAL_HUMAN_REVIEW_STATUS = PENDING_EXACT_PACKAGE_REVIEW
```

## Final

**`READY_FOR_PRIMARY_A_EXACT_PACKAGE_REVIEW`**

No email sent. No LinkedIn message or connection request. No call. No SMS.
No second enrichment provider. No API key exposed. Pilot unchanged: one
company, one recipient, one interaction, no automation, no follow-up.
