# M6.8-3 Attempt 1 — cost reconciliation note (2026-09-02)

**The immutable Attempt 1 GenerationRecords are unchanged.** They record
`input_tokens = 0`, `output_tokens = 0`, `cost_usd = 0.00` for all 27 calls
because the adapter at that time discarded provider `usage` from any response it
rejected. This note supplies the provider-estimated real charge; it does not
rewrite any record.

## Basis for the estimate

Attempt 2 (2026-09-02) ran the **same** frozen corpus, prompt template
(`comm.prompt_template.first_contact@2`, sha256 `badc6cbb1e00…`), system prompt,
and per-envelope repeat structure. The only configuration difference is
`thinking` (default vs disabled), which does not change the request payload size.
Attempt 2's **measured** usage over the same 27 calls:

- aggregate input tokens: **149,721** (≈ 5,545 / call)
- aggregate output tokens: **54,000** (27 × 2,000 — both attempts were truncated
  at the 2,000 output ceiling: Attempt 1 on thinking tokens, Attempt 2 on JSON)

## Attempt 1 estimated charge

At the owner-confirmed price table (`claude-sonnet-5.price@2026-09-02-owner-confirmed`,
input USD 2.00 / Mtok, output USD 10.00 / Mtok):

```
input : 149,721 tok × $2.00 / 1e6  ≈ $0.299442
output:  54,000 tok × $10.00 / 1e6 =  $0.540000
        ----------------------------------------
total (27 certification calls)     ≈ USD 0.84
```

Plus two small ad-hoc diagnostic probes run during the Attempt 1 investigation
(a ~16-token "reply OK" call and one ~5,900-token full-bundle inspection call):
≈ **USD 0.07**. Combined Attempt 1 session ≈ **USD 0.91**.

All well under the USD 10.00 hard ceiling.

## Fix status

`comm.anthropic_provider_adapter@1` was corrected on 2026-09-02 (commit `5de989c`):
`ProviderError` now carries `input_tokens` / `output_tokens` / `request_id` /
`served_model`, and the orchestrator bills the returned usage even when 0
candidates parse. Provider outcome and provider cost are separate fields.
Attempt 2's recorded cost (`USD 0.839442`) is therefore exact. Attempt 1's
records stay as-is by design; this note is the reconciliation.
