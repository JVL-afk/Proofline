# ADR-0078: M6.9 First-Contact Launch Review

- **Status:** **SIGNED — `GO_WITH_BOUNDED_FIRST_CONTACT_EXCEPTION` (Path B),
  scope only.** PROJECT_OWNER selected Path B on 2026-09-04/05 ("M6.9 PATH B
  APPROVED FOR ONE BOUNDED A-PLUS FIRST-CONTACT PILOT"). **This signs the launch
  *path*, not a ready-to-send package** — the pilot package itself is
  `NOT_READY_FOR_FINAL_SEND_DECISION`; see
  `docs/readiness/communication-layer/m6.9-pilot-package-a-plus-2026-09-04.md`
  for the exact remaining blockers. No email has been sent. No recipient has
  been resolved. No demo has been deployed.
- **Date drafted:** 2026-09-04. **Date signed:** 2026-09-04/05.
- **Decision owners:** PROJECT_OWNER (signed)
- **Related:** ADR-0077 (evidence-bound communication transformer), ADR-0079
  (M6.5 one-link first-contact exception, bound to this signature), ADR-0073
  (shadow person-contact evaluation, proposal), M6.5 delivery policy,
  M4 `demo.builder_brief@3.2` (frozen presentation layer),
  M6.8-4 real-company shadow evaluation (`docs/readiness/communication-layer/m6.8-4-real-company-shadow-2026-09-04.md`)
  + E+M demo gap closure (`docs/readiness/communication-layer/m6.8-4-em-demo-gap-closure-2026-09-04.md`)
  + the pilot package (`docs/readiness/communication-layer/m6.9-pilot-package-a-plus-2026-09-04.md`)
- **Supersedes:** None

## Purpose

M6.8-4 completed an internal, human-reviewed shadow evaluation of the complete
proposed interaction package (real frozen evidence → M5/M6.8 communication →
frozen M4 demo brief) for A-Plus, Elite and E+M. This ADR was the decision
package the PROJECT_OWNER + ChatGPT reviewed; the PROJECT_OWNER has now
**explicitly chosen and signed Path B**, scoped exactly as follows.

## Decision (signed)

**`M6.9 DECISION = GO_WITH_BOUNDED_FIRST_CONTACT_EXCEPTION`**

**Scope, exactly as authorized — nothing broader:**

- company: **A-Plus Air Conditioning & Home Solutions** only;
- opportunity: commercial HVAC inbound intake / qualification;
- **1** recipient; **1** first-contact message; **1** exact demo URL (governed
  by ADR-0079); **0** automatic follow-ups; **0** sequences; **0** bulk sending;
- any extension beyond this exact scope requires a new PROJECT_OWNER approval.

**M6.8-3 exception, acknowledged not waived:** the PROJECT_OWNER knowingly
accepts a one-contact exception because the standing evidence attributes the
remaining formal blocker to deterministic canonical-reconciler completeness
(§1 below), not an observed unsafe provider-output family. **This does not waive
M6.8-3 for future automation or scale.** Before any autonomous or bulk outreach,
the deferred work remains required: generative/POS-morphology-aware canonical
reconciliation, strength-increase narrowing, checkpoint/resume audit-chain
seeding, and a clean formal certification run.

**Demo-link authority, approved narrowly:** `ONE_NO_TRACKING_DEMO_URL_FOR_EXACT_APPROVED_FIRST_CONTACT`,
implemented as a **narrowly scoped, versioned M6.5/ADR-0043 exception** —
ADR-0079 v1 — not a general delivery-policy change. See §3 (below, unchanged)
and ADR-0079 for the exact permitted/prohibited conditions.

**This signature does not, by itself, make any send ready.** Contact resolution,
demo hosting, sender identity, postal disclosure, opt-out mechanism, and A-17
legal clearance remain independently required — see the pilot package doc for
the exact current state of each.

**Complete three-company M6.8-4 picture** (E+M demo gap closed 2026-09-04):

| company | communication | demo (`demo.builder_brief@3.2`) | combined |
|---|---|---|---|
| A-Plus | `SHADOW_READY_FOR_LAUNCH_REVIEW` | `BESPOKE_DEMO` (response commitment) | **the one package worth launch review** |
| Elite | `SHADOW_SAFE_BUT_WEAK` | `BESPOKE_DEMO` (service availability) | not a launch candidate — demo personalizes, communication does not |
| E+M | `NOT_DISTINCTIVE_ENOUGH` | `GENERIC_CAPABILITY_DEMO` (no evidence-shaped context; hidden-name test fails) | not a launch candidate — non-distinctive at **both** layers |

This ADR does **not** decide anything. It surfaces the decisions that require a
PROJECT_OWNER signature and binds the exact artifacts a future first contact
would use.

## 1. The unresolved M6.8-3 status (stated prominently, not blurred)

> ### M6.8-3 FORMAL STATUS: `NOT_CERTIFIED`
>
> **Reason:** a deterministic **canonical-reconciler completeness limitation** —
> `comm.canonical_claim_reconciler@2` uses an enumerated glue vocabulary with a
> structural long tail; it cannot always fully reconcile ordinary rendered
> clauses (coverage < 100% / unresolved-substantive-claim findings). This is a
> limitation of **our deterministic gate**, not an observed provider safety
> failure.

**Standing evidence (recorded, not blurred against the formal status):**

- **0 genuine provider safety violations** across the accepted live shadow
  evidence — 162 live `claude-sonnet-5` calls in M6.8-3 (all adversarial traps
  clean) **plus** the 8 M6.8-4 real-company generations (every truth / UNKNOWN /
  disclosure / demo-status / availability≠response constraint held).
- **Strong commercial-quality results** — M6.8-3 quality STRONG; M6.8-4 A-Plus
  ACCEPTABLE 0.86 ×4, uses strongest hook, company-specific.
- **Formal `CERTIFIED_SAFE` still not achieved** — the reconciler completeness
  gate is not met, and the checkpoint/resume audit-chain seam is not yet sealed.

These two facts stand side by side: the model's behaviour is safe on the
standing evidence; the formal certification is not complete.

## 2. Two legitimate launch paths (the owner chooses; this ADR does not)

### PATH A — FORMAL-CERTIFICATION-FIRST

**NO-GO** until the deferred M6.8-3 work is completed:

1. POS / morphology-aware **generative canonical reconciler** (replaces the
   enumerated-glue reconciler; must NOT be gate-fit by adding words to the glue
   set);
2. **strength-increase narrowing** (the `strength_increase` false-positive class);
3. **checkpoint / resume audit-chain seeding** (seal the resume seam so a
   `CERTIFIED_SAFE` run has one continuous hash chain);
4. one clean bounded **`CERTIFIED_SAFE`** `claude-sonnet-5` run.

Then return to M6.9 with a certified transformer and re-run this review.

*Cost:* an owner-authorized, bounded engineering task (reconciler rework) +
one more paid certification run. *Benefit:* first contact proceeds only behind a
formally certified transformer.

### PATH B — BOUNDED HUMAN-REVIEWED FIRST-CONTACT EXCEPTION

Permit **exactly one** real first contact despite M6.8-3 remaining formally
`NOT_CERTIFIED`, **only if** the PROJECT_OWNER explicitly signs an exception
acknowledging the formal blocker. This path is extraordinarily bounded:

- exactly **one recipient**;
- exactly **one company** — the complete M6.8-4 matrix points to **A-Plus** as
  the only package distinctive and coherent at both the communication and the
  demo layer; Elite is `SHADOW_SAFE_BUT_WEAK` (demo `BESPOKE`, communication
  weak); E+M is non-distinctive at both layers (`NOT_DISTINCTIVE_ENOUGH`
  communication + `GENERIC_CAPABILITY_DEMO`);
- **exact-version-bound** communication artifact (bound to `body_sha256`);
- **exact-version-bound** demo brief (`demo.builder_brief@3.2`, bound to
  `underlying_brief_sha256`);
- **human review of every rendered word** immediately before use;
- **no automated sequence**; no autonomous regeneration after approval;
- **no tracking**; no bulk sending; no automatic follow-up; no CRM automation;
- **kill switch remains available** at all times;
- **any content change invalidates the approval** and requires a fresh signature.

*Cost:* the owner personally accepts the formal blocker for one bounded test.
*Benefit:* a real first-contact signal without waiting on the reconciler rework.

**This ADR does not choose between A and B.** M6.9 exists to make that owner
decision explicit and signed.

## 3. Demo-link policy conflict — explicit owner decision required

> **RESOLVED 2026-09-04/05: Option 2 selected —
> `AUTHORIZE_ONE_NO_TRACKING_DEMO_URL_FOR_EXACT_APPROVED_FIRST_CONTACT`.**
> Implemented as **ADR-0079** (a narrowly scoped, versioned M6.5/ADR-0043
> exception bound to exactly this pilot), not a general M6.5 change. The URL
> itself does not yet exist — no demo has been rendered or hosted (pilot package
> §2/§7).

The M6.5 delivery policy previously prohibited **links, attachments, and
tracking**. The new customer experience (M4 V3.x) depends on an **interactive
demo**. M6.9 must **not silently assume demo links are allowed.**

> ### DECISION REQUIRED: `DEMO_LINK_AUTHORITY`
>
> - **Option 1 — `NOT_AUTHORIZED`** (default): the first contact contains **no
>   demo link**. The email describes the simulation concept in prose only; a
>   demo is shown later, in a separate channel, only if a reply invites it and a
>   further owner decision authorizes it.
> - **Option 2 — `AUTHORIZE_ONE_NO_TRACKING_DEMO_URL_FOR_EXACT_APPROVED_FIRST_CONTACT`**:
>   one demo URL may be included, and **only** if it:
>   - points only to the **exact approved demo** (the `demo.builder_brief@3.2`
>     A-Plus rendering, bound by hash);
>   - contains **no tracking parameters** of any kind;
>   - contains **no personalized secret / customer data** in the URL;
>   - does **not imply target-business ownership or affiliation**;
>   - remains **clearly labelled** "Simulation · not connected to {{business}}";
>   - does **not collect** real customer / contact data;
>   - contains **no analytics** unless separately approved.

**This ADR does not alter the M6.5 delivery policy.** It only surfaces the
decision. If Option 2 is chosen, a separate change to M6.5 is required first.

## 4. Exact-artifact binding (the approval binds hashes, not descriptions)

Any M6.9 approval binds the following exact identities. **Any mutation of any
bound artifact after approval invalidates the approval.**

| field | value (A-Plus, from M6.8-4) |
|---|---|
| target company | A-Plus Air Conditioning & Home Solutions (`www.aplusac.com`) |
| target opportunity | Texas commercial-HVAC inbound structured-intake, anchored on the published response-positioning signal |
| source evidence bundle | envelope `c5a6307f4427d450359946b87d64cecd43f090ed28946c90bd175eaa2b0b380e`; m2–m5 bundle `a6540437f096ac7b6d0e026fd75ebdffbd5a92a1b77f59e730fb3ee24b3554e7` |
| M4 version | `demo.builder_brief@3.2` (FROZEN); A-Plus brief `underlying_brief_sha256 4d9f26412893d85fc21473b65e50466433d3655275957ad3523b20323538636a` |
| M5 / M6.8 communication version | prompt `@9` (`PROMPT_TEMPLATE_ID_CERT_V9`); `OutputValidator(contract="v3")` = `comm.output_validator@7`; `comm.cta_parser@3`; `comm.canonical_claim_reconciler@2` |
| selected communication artifact | `body_sha256 184cb2ca18d680d08e7708fdae967e851e2dff53dc0c1394acc7403133e3f419`; subject "A quick question about A-Plus AC's intake process" (from `m6.8-4-real-shadow-2026-09-04T111024Z.json`) |
| rendered demo URL | **Option 2 selected; no URL exists yet** — no demo has been rendered/hosted; will bind by URL + content hash once available |
| human reviewer | *(still to be named)* |
| PROJECT_OWNER | jvlmanus@gmail.com |
| send channel | email only; no phone / SMS / LinkedIn (confirmed by the owner's decision) |
| recipient / contact evidence | **STILL NOT RESOLVED** — contact resolution's own precondition (all prerequisites satisfied) is not yet met; see pilot package §3 |
| timestamp / validity / expiry | *(still to be set once a package is actually ready)* |
| kill-switch status | must read ARMED at send time (unassessed — package not yet at that stage) |
| legal / privacy review status | **NOT SATISFIED** — no postal disclosure, no opt-out mechanism, and **A-17 has never been signed** (pilot package §4) |
| M6.8-3 formal status acknowledgment | `NOT_CERTIFIED` — **acknowledged by the owner's signed Path B decision** |
| M6.8-4 shadow result | A-Plus `SHADOW_READY_FOR_LAUNCH_REVIEW` |
| approval decision | **`GO_WITH_BOUNDED_FIRST_CONTACT_EXCEPTION` (Path B) — SIGNED, scope only; package NOT_READY (see pilot package doc)** |

## 5. Contact-eligibility prerequisites (defined here; not started)

Contact resolution **remains entirely out of scope**. No search for owner /
president / GM / operations / service manager / email / phone / LinkedIn / any
recipient occurs until a signed GO path explicitly authorizes it.

M6.9 **may define** the future contact-eligibility requirements:

- a single, role-appropriate decision-maker (owner / GM / operations lead) at the
  exact target business;
- a **publicly listed business** contact channel (no scraped personal address);
- verification that the recipient and the business match the frozen envelope
  identity (display name + hostname);
- no prior opt-out / suppression;
- the recipient bound to the exact approved artifact hash before final review.

## 6. Exact future first-contact lifecycle (planning only — not executed)

```
GO signed (Path B, or Path A completed + re-reviewed)
  -> resolve exactly one eligible decision-maker (public channel only)
  -> verify recipient identity matches the frozen envelope
  -> bind the exact recipient to the approved artifact hash
  -> final human review of every rendered word (subject + body + any link)
  -> single controlled send (one recipient, one message, email only)
  -> observe result
  -> NO automated follow-up without a separate owner approval
```

Each arrow is a manual, human-gated step. There is no automation, no sequence, no
regeneration, no tracking at any step.

## 7. Decisions — status after the 2026-09-04/05 signature

1. ~~**Launch path**~~ — **RESOLVED: Path B, signed.**
2. ~~**`DEMO_LINK_AUTHORITY`**~~ — **RESOLVED: Option 2, via ADR-0079.**
3. ~~**If Path B, which company + M6.8-3 acknowledgment**~~ — **RESOLVED:
   A-Plus; `NOT_CERTIFIED` acknowledged, not waived.**
4. **Disclosure-slot resolution — STILL OPEN.** Attempted against authorized
   project configuration; **none of the five slots
   (`{{verified_sender_signature}}` / `{{required_postal_disclosure}}` /
   `{{approved_opt_out_instruction}}` / `{{functional_role_or_team}}` / the demo
   URL) could be resolved** — the underlying real-world assets do not exist in
   this project. See pilot package §2 for exactly what the PROJECT_OWNER must
   supply for each.
5. **Legal / privacy review sign-off — STILL OPEN**, and the hardest blocker:
   **A-17 has never been activated** for this project (a standing M6/M6.5/M6.7
   deferred blocker, not something this task can resolve). See pilot package §4.
6. **Reviewer + send channel + validity window** — send channel confirmed
   (email only); reviewer and validity window still to be named once the
   package is otherwise ready.

**Net effect: the launch-path decision is made; the pilot package is not yet
executable.** Contact resolution (ADR-0078 §5/§6) has not been attempted because
its own precondition — items 4 and 5 above — remains unsatisfied.

## Consequences

- **Positive:** the launch decision is explicit, signed, and bound to exact
  hashes; the M6.8-3 blocker cannot be silently bypassed; the demo-link policy
  conflict is surfaced, not assumed.
- **Negative:** Path B accepts a formally uncertified transformer for one contact
  on the owner's personal signature; Path A delays first contact by an
  engineering task.
- **Risk:** an owner signature on Path B without resolving the disclosure slots
  or legal review would produce a non-compliant send — mitigated by making
  slot resolution + legal sign-off explicit prerequisites (§7.4, §7.5) and by
  "any content change invalidates the approval".

## Revisit triggers

- The reconciler rework lands and a `CERTIFIED_SAFE` run completes → Path A
  resolves; re-run this review with a certified transformer.
- The owner signs Path B → the first-contact lifecycle (§6) begins, one step at
  a time, each human-gated.
- Any bound artifact changes → this ADR's bindings are stale; regenerate.
