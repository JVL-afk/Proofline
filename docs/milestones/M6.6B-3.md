# Milestone M6.6B-3 — Controlled Live Candidate Certification

**Status:** Complete — certification checkpoint recorded; Tournament II remains blocked
**Authorized:** 2026-08-19
**Executed:** 2026-08-19
**M6.6B-2 authority:** `ec8b853a39906440b47db5b0c0c098befb17204d`
**Draft manifest:** `f0a896720cb519625d5aa42a435a247388a5dbd51818938800f040e9907c3cf7`

## Boundary

M6.6B-3 is a one-shot schema, transport, usage-metadata, and model-lineage certification. It may
make at most five logical calls, each with one retry only after an unambiguous transient where no
model result was accepted. It is governed by ADR-0054, ADR-0057, ADR-0058, ADR-0060, ADR-0061, and
ADR-0062. It cannot access Tournament II partitions, score intelligence, freeze a candidate, mark
readiness, activate a route, or change M1-M6.5 authority.

The exact calls are Sol semantic reasoning, Sonnet semantic reasoning, Sonnet controlled wording,
Gemini 3.6 controlled wording, and Gemini 3.5 Flash-Lite objective extraction. The approved endpoints
are OpenAI `/v1/responses`, Anthropic `/v1/messages`, and Google stable `/v1/interactions`. Model IDs,
schema hashes, reasoning controls, output ceilings, storage controls, and tool denial are inherited
unchanged from the closed M6.6B-2 manifest.

## Authorization and safety

The authorization binds the exact M6.6B-2 commit and manifest hash, four-deployment roster, five call
IDs, three endpoints, execution actor/window, one-use nonce digest, dedicated synthetic fixtures,
schema-only purpose, USD 1 ceiling, and armed global/budget kill switches. The runner rejects hidden
or tournament access structurally. Every outbound transport enforces HTTPS, an exact provider host,
and same-allowlist redirects. Unexpected egress is a fatal stop, not a retry.

Credentials are read only by the explicit local runner from the ignored file, passed only in the
matching provider header, and never included in prompts, hashes, exceptions, logs, or artifacts.
Provider outputs are inert untrusted data. Raw request and response bodies are discarded after
immediate validation. Persisted receipts contain safe hashes, schema/configuration lineage, returned
identity, request/response IDs where supplied, status/finish information, token counters, latency,
dated pricing, calculated cost, and safe failure codes.

## Dedicated synthetic fixtures

Three minimal fixtures exercise semantic reasoning, controlled wording, and objective extraction.
They contain only reserved `cert-*` identifiers and a synthetic business marker. They require exact
preservation of a protected unknown, contradiction and/or deterministic safety label, and contain no
real business, website, person, contact, reply, M1-M6 record, hidden case, or rotating challenge.
Schema validity and deterministic reference preservation are evaluated; model quality is not scored.

## Pricing preflight

Official pricing was refreshed on 2026-08-19. The unchanged five-call maximum is USD 0.145467. The
authorization reserves USD 0.290934 by conservatively charging the full maximum again for every
permitted retry, below the USD 1 hard ceiling. Unknown pricing, source drift, or a ceiling breach
stops execution before a call. The dated source record is
[`official-pricing-preflight-2026-08-19.json`](../evidence/m6.6b-3/official-pricing-preflight-2026-08-19.json).

## Identity policy

An exact returned `claude-sonnet-5` is `IDENTITY_SUFFICIENT` under the provider-documented pinned
identity. Exact OpenAI and Google stable IDs remain `IDENTITY_CONDITIONAL`; certification records
their exact returned metadata but does not decide that a stable name is immutable. A missing or
mismatched returned identity is `IDENTITY_INSUFFICIENT`. The weaker-binding governance decision
remains human-owned.

## Outcome policy

Each deployment receives `CERTIFICATION_PASS`, `CERTIFICATION_CONDITIONAL`, or
`CERTIFICATION_FAIL`. A call fails for transport/provider rejection, malformed output, schema or
semantic invalidity, observed tools, or insufficient identity. Sonnet remains a candidate only if
both schema families pass. Failure remains schema/task local unless separately reviewed evidence
establishes provider-wide or configuration-wide blast radius.

## Exclusions

No hidden or rotating qualification case, safety gauntlet, subjective review, application data,
real entity/contact/reply, prompt repair, schema relaxation, alternate endpoint/model, provider
setting mutation, Tournament II execution, candidate freeze, `READY_FOR_EXECUTION`, AI route,
M6.7, delivery, or autonomous behavior is authorized.

## Certification result

The consumed authorization attempted exactly five logical calls and made five transport attempts;
no retry was eligible or used. Three calls completed. OpenAI Sol accepted the exact semantic schema,
returned `gpt-5.6-sol`, preserved the deterministic references, exposed ordinary response/usage
metadata, and is `CERTIFICATION_CONDITIONAL` because the returned stable name does not establish an
immutable deployment revision. Anthropic Sonnet accepted both the semantic-reasoning and
controlled-wording schemas, returned the documented pinned `claude-sonnet-5` identity for both,
preserved the deterministic references, and is `CERTIFICATION_PASS` for this transport/schema
checkpoint.

Both Google stable `/v1/interactions` calls returned HTTP 400 with the safe code `invalid_request`.
No body was retained, no prompt/schema/configuration was changed, and no retry or alternate endpoint
was attempted. Therefore Gemini 3.6 Flash controlled wording and Gemini 3.5 Flash-Lite objective
extraction are `CERTIFICATION_FAIL` on the exact draft configuration. The failure is local to each
certification binding; it does not establish a provider-wide safety failure.

The five-call calculated spend was USD 0.014224: Sol USD 0.006250, Sonnet reasoning USD 0.004960,
Sonnet wording USD 0.003014, and both rejected Google requests USD 0.000000 under returned usage.
All completed calls reported zero cached and zero separately exposed reasoning/thinking tokens.
The authorization closed as `CONSUMED`; its safe receipt is
[`certification-run-2026-08-19.json`](../evidence/m6.6b-3/certification-run-2026-08-19.json).

Account access and endpoint entitlement were demonstrated for Sol and Sonnet only. No separate
rate/tier, ZDR, or account-control-plane retention state was exposed by these ordinary responses.
The runner requested non-persistence where the approved endpoint supports that request; manual
account-control verification remains a blocker. Google endpoint/configuration acceptance remains a
blocker. OpenAI's weaker version-lineage contract remains a human decision. No candidate is frozen,
no manifest is ready, and no application route is activated.
