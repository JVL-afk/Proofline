# Milestone M4 — Personalized Demo Engine

**Status:** Complete — deterministic local slice; production gates remain open
**Authorized:** 2026-08-17
**Completed:** 2026-08-18

## Objective

Generate a deterministic, evidence-grounded simulation for the accepted Texas Commercial HVAC
inbound lead-response opportunity and an exact approved M3 audit, using only vetted components,
synthetic data, mock-only actions, and a separate capability-scoped runtime.

## Authority and exclusions

M1-M3 remain canonical and read-only to M4. Live AI, real integrations, public sharing, export,
recording/media generation, outreach, arbitrary generated code, and M5 behavior are prohibited.

## Exit criteria

- [x] Exact accepted-M2 and approved-M3 eligibility fails closed.
- [x] Immutable manifests and revisions preserve complete version lineage.
- [x] One canonical scenario produces interactive, conversational, cue-sheet, and technical views.
- [x] Components, transitions, guards, events, and actions are registered and deterministic.
- [x] All personas and lead inputs are synthetic; actual contact or sensitive data is rejected.
- [x] Every operational receipt is structurally `MOCK_ONLY` and no real adapter exists.
- [x] Runtime-owned disclosure is persistent and cannot be altered by specifications.
- [x] Separate-origin sessions enforce 15-minute issuance and 60-minute runtime limits.
- [x] Revision/session revocation and upstream invalidation fail closed.
- [x] Minimal telemetry excludes text, contact data, tokens, evidence bodies, and transcripts.
- [x] Hard QC, review, lineage, replay, authorization, and hostile fixtures pass.
- [x] Complete M0-M4 deterministic validation passes without live AI or uncontrolled network.
- [x] No M5 functionality or route is introduced.

## Implemented local slice

M4 adds typed domain/application/port boundaries, append-oriented SQLite demo revisions and review
history, a read-only canonical M1-M3 source catalog, leased deterministic composition, immutable
hashed manifests/specifications, a vetted component/question/action registry, a bounded replayable
state machine, synthetic personas, fixed human/safety handoffs, mock-only action receipts, hard QC,
exact review, upstream invalidation, revision/session revocation, 15-minute one-time capabilities,
60-minute runtime sessions, and allowlisted metadata-only telemetry.

The separate `demo-runtime` service has no database or provider/research/application credential. It
renders the runtime-owned disclosure with a strict no-external-connection CSP and exchanges only
the narrow capability/session contracts with the core service. The diagnostic application exposes
manifest, specification, lineage, QC, review, and private launch controls. No public/share/export,
recording/media, live AI, outreach, or real operational adapter exists.

## Validation

Closure validation used Node.js 24.15.0, npm 11.12.1, Python 3.13.3, and locked uv 0.11.32.
Repository validation, Ruff lint/format over 127 files, strict mypy over 94 source files, all 89
deterministic M0-M4 tests, Python byte-compilation, both diagnostic JavaScript syntax checks, npm
clean install, and `git diff --check` passed. Tests used no provider credential, live AI, browser
network, uncontrolled website, public sharing, or real side-effect integration.

The final closure review on 2026-08-18 also confirmed that the working tree contains only the
intentional M4 slice, M1-M3 canonical package files are unchanged, no M5 or public/export route is
present, every operational action remains structurally `MOCK_ONLY`, and expiry, revocation, the
global issuance kill switch, runtime isolation, and CSP controls remain enforced.

## Builder-brief trim (owner + ChatGPT sign-off 2026-09-03)

The declarative M4 demo authority is **unchanged**. Additive only:

- `opintel_demo.scenario` — `demo.scenario_skeleton@1`, a read-only accessor for
  the fixed business-independent `commercial_hvac.inbound_lead_response` scenario
  (states / transitions / question set / mock actions / handoff conditions /
  safeguards / deployment status). Every value is re-exported from
  `opintel_demo.composition` / `opintel_demo.policy`; no composition or QC
  behaviour changes.
- `opintel_communication.builder_brief` — `demo.builder_brief@1`, a deterministic
  (no-LLM) compiler: `(frozen SemanticEnvelope + scenario skeleton) → BuilderBrief`
  → paste-ready `.md` for a website/app builder (Lovable etc.). The builder is a
  presentation renderer only; a fail-closed `_assert_within_authority` check
  rejects any brief that would hand the builder business truth (integrations,
  CRM, response times, staffing, economics, internal process). Demo modes
  `BESPOKE / EVIDENCE_BOUND / GENERIC_CAPABILITY / NOT_DISTINCTIVE_ENOUGH`;
  deterministic anchor ranking.
- First and only target generated: **A-Plus** (`BESPOKE_DEMO`, anchor =
  `response_commitment`). Manual workflow only — no Lovable API, no automation,
  no deployment. `tests/test_m4_builder_brief.py` (15 tests, the 11 named
  section-10 invariants).
  Evidence: `docs/readiness/demo-builder-brief/m4-builder-brief-implementation-evidence-2026-09-03.md`.

### V2 — human-experience simplification (owner authorization 2026-09-03)

- `opintel_communication.builder_experience` — `demo.builder_brief@2`, a
  presentation projection of the V1 brief into a **3-screen ~60-second
  experience** (Opportunity / one-page simulation input / Result-handoff; + 3
  exception screens on trigger). Fictional demo placeholders
  (`FICTIONAL_DEMO_PLACEHOLDER`, role > name, always disclosed), internal
  ontology hidden, mock actions shown as "Prepare …" previews, quiet disclosures.
  A 40–50 word deterministic opportunity summary (evidence + proposed simulation
  + explicit unknown). `_assert_experience_within_authority` (12 checks on top of
  the V1 M4-authority check). **No `opintel_demo` file changed; V1 unchanged.**
- `tests/test_m4_builder_brief_v2.py` (20 tests, the 18 named section-18
  invariants). Full suite **1162 pass**.
  Evidence: `docs/readiness/demo-builder-brief/m4-builder-brief-v2-implementation-evidence-2026-09-03.md`.

### V3 — opportunity story (owner authorization 2026-09-03)

- `opintel_communication.builder_story` — `demo.builder_brief@3`, a further
  presentation projection of the V2 experience into a short opportunity story
  ("we noticed this -> how it could work -> what your team could receive ->
  imagine it fitted to your real process"). Optimises externally for
  comprehension + relevance + desire while M4 keeps optimising internally for
  truth + safety + provenance. Changes vs V2: Screen 1 title **is** the
  opportunity headline (no demo-mode badge in the UI); evidence detail collapsed
  ("Why we built this"); new `SYNTHETIC_DEMO_INPUT` vs `PUBLIC_EVIDENCE` /
  `EVIDENCE_CONTEXT` distinction so an evidence phrase is never a selectable form
  value (retires the V2 "known tension"); Screen 3 is a 4-stage workflow story;
  the six mock actions grouped into three customer outcomes (all six preserved
  verbatim underneath); prominent closing bridge; `builder_visual_guidance` +
  `company_visual_character` + a plain-language `semantics_legend`.
  `_assert_story_within_authority` (15 checks on top of the V1 + V2 checks).
  **No `opintel_demo` file changed; V1 + V2 unchanged.**
- `tests/test_m4_builder_brief_v3.py` (18 tests, the 16 named section-19
  invariants). Full suite **1180 pass**.
  Evidence: `docs/readiness/demo-builder-brief/m4-builder-brief-v3-implementation-evidence-2026-09-03.md`.

### V3.1 — cleanup + Elite personalization proof (owner authorization 2026-09-04)

- `demo.builder_brief@3` → `demo.builder_brief@3.1` (same module, presentation
  cleanup only). The ontology legend leaves the default customer view (audit /
  developer note only); Screen 2 fields show a short `customer_hint` (the
  evidence/safety rationale stays in the audit layer); new `builder_hard_rules`
  + `allowed_primary_actions` forbid invented functionality (exports, downloads,
  PDF, share/save, analytics, dashboards, state selectors, demo badges) — only
  the two enumerated actions are interactive; new per-anchor `scenario_framing`
  + `result_recap_line`; the opportunity headline is guaranteed anchor-tied and
  cannot imply verified performance. 7 new checks (21 total).
  `tests/test_m4_builder_brief_v3.py` now 27 tests. Full suite **1189 pass**.
- **Elite personalization proof:** the same `compile_builder_story` run on
  `tests/m68_fixtures.elite_envelope()` (frozen, no re-research) — Elite anchors
  on **service availability** (A-Plus anchors on **response commitment**),
  different headline / summary / scenario framing / quoted evidence.
  **`PERSONALIZATION_PROOF_PASSED`** — distinctive at the opportunity layer;
  intake mechanics deliberately shared (section 14). Availability is context
  only, never upgraded to a response claim.
  Docs: `m4-builder-brief-v3.1-implementation-evidence-2026-09-04.md`,
  `m4-aplus-vs-elite-personalization-2026-09-04.md`,
  `m4-elite-builder-brief-v3.1-2026-09-04.{json,md}`.
  **No `opintel_demo` file changed; V1 + V2 unchanged.**
  **STOPPED for PROJECT_OWNER + ChatGPT review**; owner will manually paste Elite
  into Emergent. No E+M, no M5/M6.8 change, no M6.8-4, no contact.

### V3.2 — FINAL FREEZE (owner authorization 2026-09-04, Part A)

- `demo.builder_brief@3.1` → **`demo.builder_brief@3.2`**, then **frozen**
  (`M4_PRESENTATION_FROZEN = True`). Two manual UX discoveries formalized
  generically: **`example_scenarios`** (4 optional Screen-2 presets that fill the
  existing synthetic fields — every value a real `SYNTHETIC_DEMO_INPUT` option,
  no new authority, never a company fact) and **`start_over`** (a post-result /
  post-exception local state reset — saves/sends/tracks/persists nothing). The
  customer-facing control list is closed to six items; builder-invented
  functionality (PDF/download/save/share/email/analytics/dashboards/integrations/
  booking/CRM/deployment) stays forbidden. 3 new authority checks (24 total).
  `tests/test_m4_builder_brief_v3.py` 34 tests; full suite **1196 pass**.
  **No `opintel_demo` file changed; authoritative M4 demo semantics unchanged;
  A-Plus + Elite generated through the same compiler; builder rendering manual;
  no external builder in the authority chain.**
  Evidence: `docs/readiness/demo-builder-brief/m4-presentation-freeze-2026-09-04.md`.
  **M4 presentation layer is FROZEN — no further aesthetic tuning.**

## Formally deferred production decisions

- Production cloud/region/IaC, runtime domain, service identity, gateway, and network policy.
- Production OIDC and separation of duties; local self-review remains explicitly attributed.
- Demo/telemetry retention, consent, IP-address treatment, backup, and recovery policy.
- Any logo, trademark, copied brand asset, custom component, or additional opportunity definition.
- Public links, anonymous access, export/download, recording/media generation, or distribution.
- Real CRM/calendar/email/SMS/phone/booking/dispatch/notification integration or target deployment.
- Live AI composition/conversation and final provider/model selection.
- M5 proposal, outreach, delivery, or autonomous behavior.
