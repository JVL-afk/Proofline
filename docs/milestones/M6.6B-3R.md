# Milestone M6.6B-3R — Gemini Certification Repair

**Status:** Complete — successor schema certification passed; identities remain conditional
**Authorized:** 2026-08-19
**Executed:** 2026-08-19
**M6.6B-3 closure:** `c787e0961c31fefca87cd46f09fdef32497bcb73`
**Immutable predecessor evidence blob:** `7b2d8c6ce1f162c42a5a6fb5b3643636cc332ea1`

## Boundary

M6.6B-3R is not Tournament II. It may certify only corrected successors to the two failed Google
candidate revisions, using the same synthetic fixtures and semantic validators. It cannot call
OpenAI or Anthropic, access hidden/rotating/real data, change models/tasks/schemas, tune wording or
intelligence, create another successor automatically, freeze a candidate, mark readiness, activate
a route, or change M6/M6.5/M6.7 authority.

The original M6.6B-3 receipt is immutable experimental evidence. The repair stores separate lineage
and never rewrites the predecessor artifact.

## Documentation-first diagnosis

The current stable v1 request reference lists `tool_choice` inside `generation_config`. Both rejected
M6.6B-3 requests serialized `tool_choice: "none"` at the top level. Their other material fields match
the current contract: top-level polymorphic `response_format` with `type: "text"`,
`mime_type: "application/json"`, and nested `schema`; `max_output_tokens`, `thinking_level`, and
`thinking_summaries` inside `generation_config`; exact stable model IDs; `store=false`; and
`background=false`. The schemas use only documented supported JSON Schema features.

The diagnosis is `OTHER_REQUEST_CONTRACT_ERROR`: a misplaced supported field. Confidence is
`documentation_demonstrated_provider_detail_not_retained`, because the original safe receipt kept
only `invalid_request` and intentionally discarded Google's raw diagnostic. A successful successor
would strongly corroborate the diagnosis but would not retroactively rewrite it.

Google documents stable v1 support for Interactions, structured output, and thinking. The successor
therefore preserves `https://generativelanguage.googleapis.com/v1/interactions`; no version switch or
exception is needed. Source details are frozen in
[`official-google-contract-2026-08-19.json`](../evidence/m6.6b-3r/official-google-contract-2026-08-19.json).

## Immutable successor revisions

| Successor | Revision hash | Predecessor config | Successor config |
| --- | --- | --- | --- |
| Gemini 3.6 wording `@2` | `765c611fde33be82c42f338e98dfa3d0985448680b32cebf53af92d2162c9f7b` | `abaa2ade613f4dbacab2d9491b0d2e442c55cfa14b5ab61ca41ea3bc0debcee6` | `fd40373329037b3159b0614f90c6c6f6bfb0abd0a1bf18da864325616cef1d56` |
| Flash-Lite extraction `@2` | `ef69fb51c63672311dda446dbd1357d0c194f9a7bb4aa580d881f5ff6ab04206` | `7f22e18d917d297ed7470fc9b7acc550e7686e364819a00aa318e3cf4b8a293a` | `2dba56adf1ff53caebc27776042275a477917ef12c80e7604aeaf516131800fc` |

The exact semantic diff for both successors is:

- Remove top-level `tool_choice: "none"`.
- Add `generation_config.tool_choice: "none"`.
- Change nothing else: endpoint, API version, model, synthetic input, task, schema and hash, output
  ceiling, `store`, `background`, thinking level, thinking summaries, and semantic validation remain
  exact.

No schema transformation is used. Gemini 3.6 retains `thinking_level=low`; Flash-Lite retains
`thinking_level=minimal`. Neither request contains legacy `thinking_budget`, deprecated sampling
parameters, tools, files, search, code, function, or computer configuration.

## Authorization and budget

The new one-shot authorization binds the M6.6B-3 closure commit and evidence blob, both successor
revision IDs/hashes, two call IDs, exact models, Google provider and stable v1 endpoint, synthetic
schema-only purpose, actor/window/nonce digest, and armed global/budget kill switches. It explicitly
denies hidden/Tournament access and OpenAI/Anthropic calls.

At current prices, the two-call maximum is USD 0.013289. Reserving the full maximum once again for
each permitted transient retry is USD 0.026578, below the USD 0.25 hard ceiling. A semantic or HTTP
400 rejection is not retryable.

## Safe failure observability

Failure receipts retain HTTP status, safe provider code, provider request ID where returned,
candidate revision, endpoint, requested model, timestamp, and strictly normalized rejected-field
diagnostics. Raw provider messages, requests, responses, prompts, inputs, and credentials are never
persisted. A second request-contract failure stops the revision; no third revision is generated.

## Exit

After at most two primary calls, the authorization closes, cost is reconciled, credential references
are removed, the full deterministic suite runs, and each successor receives a separate schema
certification and identity result. Even success remains `IDENTITY_CONDITIONAL` unless Google returns
qualification-grade immutable lineage. Tournament II, candidate freeze, and route activation require
subsequent human review and authorization.

## Repair result

The consumed repair authorization attempted and completed exactly two primary calls with no retry.
Both stable v1 requests returned HTTP 200 and the exact requested model ID. Gemini 3.6 Flash accepted
the controlled-wording schema; Gemini 3.5 Flash-Lite accepted the objective-extraction schema. Both
outputs parsed, rejected no required field, introduced no extra field, preserved the deterministic
unknown/contradiction/CTA or extraction references, entered the existing semantic validator, and
observed no tool/action step.

Gemini 3.6 reported 135 input, 147 output, zero separately exposed thought, zero cached, and 282
total tokens; calculated cost was USD 0.000653. Flash-Lite reported 109 input, 71 output, zero
separately exposed thought, zero cached, and 180 total tokens; calculated cost was USD 0.000211.
Total repair cost was USD 0.000864. Both responses exposed `created` and `updated` timestamps but no
separate response ID or provider request ID in the received safe metadata.

Both successors are `CERTIFICATION_CONDITIONAL`: transport, exact configuration, schema, and semantic
compatibility passed, while identity remains `IDENTITY_CONDITIONAL` because an exact returned stable
model name is not qualification-grade immutable lineage. The successful repair corroborates the
misplaced `tool_choice` diagnosis without changing the original M6.6B-3 failures or their evidence.
The consumed safe receipt is
[`gemini-repair-run-2026-08-19.json`](../evidence/m6.6b-3r/gemini-repair-run-2026-08-19.json).

Both Gemini models remain recommended as candidates for Tournament II subject to human approval of
the weaker identity contract, account-control-plane verification, complete roster freeze, reviewer
assignment, final budget approval, and a separate Tournament II authorization. No candidate is
frozen or ready, and no route is active.
