# M6.10 — Recipient & Channel Resolution Engine

Owner authorization 2026-09-06 "M6.10 RECIPIENT & CHANNEL RESOLUTION
ENGINE". Formally opens M6.10: given a qualified company/opportunity,
automatically identify the best appropriate business decision-maker(s),
discover available professional contact routes, verify them, apply
legal/privacy/policy constraints, rank eligible channels, and produce an
evidence-backed contact plan while refusing unverified or unauthorized
routes. Not an autonomous phone-calling or bulk social-outreach system.

## Status

- [x] **Architecture + engine implemented, 2026-09-06 (commit pending).**
      `docs/adr/0080-m6.10-recipient-channel-resolution.md`. New
      `opintel-contact-resolution-core`/`-local` packages: deterministic
      role ranking, 11-category source taxonomy, 6-domain
      `SourceAuthorityMatrix` (fail-closed `NOT_AUTHORIZED` default), 7-state
      email verification with monotonic threshold gating, LinkedIn
      (manual-only) and phone (human-call-only, main-line vs. direct
      distinguished) channel models, 13-state eligibility disposition
      machine, deterministic channel ranking with hard blockers, privacy
      minimization, provider-adapter Protocol + `NullSourceProvider` (no
      real provider implemented), cost-budget config, and an append-only
      `ContactEndpointEvidence` persistence layer.
- [x] **Suppression extended, not replaced.** `opintel_suppression.SuppressionKind`
      gained `PERSON_SUPPRESSED`/`CHANNEL_SUPPRESSED`/`COMPANY_SUPPRESSED`
      (additive; Stage A behavior unchanged) plus
      `suppress_person`/`suppress_channel`/`suppress_company`/
      `check_channel_blocked` on the same append-only repository.
- [x] **Tests: fixtures A-O + persistence/restart + hash stability.**
      `tests/test_m610_contact_resolution.py`, 20 tests. Full suite
      **1237 passed, 3 skipped** (pre-existing, Postgres-gated); ruff +
      ruff format + mypy strict clean across
      `opintel_contact_resolution`/`_local` and the extended
      `opintel_suppression`.
- [x] **A-Plus first real acceptance case run, 2026-09-06.**
      `scripts/run_m610_aplus_acceptance.py` (no network call — uses only
      evidence already gathered/authorized in prior M6.9 tasks),
      `docs/readiness/communication-layer/m6.10-aplus-acceptance-2026-09-06.json`.
      Recipient: **Greg Yamin, President** (first-party, `HIGH` confidence,
      via `aplusac.com/about-us/our-team/`). `EMAIL` =
      `IDENTITY_VERIFIED_CONTACT_UNRESOLVED` (no email published anywhere
      first-party; no pattern-derived candidate constructed — see ADR-0080).
      `LINKEDIN` = `CONTACT_SOURCE_NOT_AUTHORIZED` (a profile URL surfaced
      via search but the profile page itself was never fetched — HTTP 999 —
      and `LINKEDIN_PROFILE_DISCOVERY_AUTHORITY` was never granted).
      `PHONE` = `ELIGIBLE_HUMAN_CALL_INDIRECT` (the verified main business
      line, 512-450-1980 — honestly distinguished from Greg's own direct
      number, which is unknown). **Recommended channel: PHONE (human call,
      indirect).** Final:
      **`IDENTITY_VERIFIED_CONTACT_RESOLUTION_PARTIALLY_BLOCKED_BY_SOURCE_AUTHORITY`.**
      Reality wins — no endpoint was forced.
- [x] **Counsel decision artifact produced.**
      `docs/readiness/communication-layer/m6.10-counsel-decision-artifact-2026-09-06.md` —
      8 decision fields, all currently blank; counsel's answer is not
      assumed.
- [ ] **NEXT:** PROJECT_OWNER sends the counsel decision artifact to Texas
      counsel. Once (and only for the categories) counsel returns
      `APPROVED`/`APPROVED_WITH_CONTROLS`, the corresponding
      `AuthorityDomain` can be granted in a `SourceAuthorityMatrix` and a
      real provider adapter (behind the existing `SourceProviderPort`)
      implemented and activated — no change to eligibility/authority
      semantics is needed to do so. Until then: no live third-party
      contact-enrichment call, no LinkedIn profile fetch, no direct-phone
      resolution. The A-Plus pilot remains paused at recipient resolution;
      M6.9 Path B, M6.8-3 `NOT_CERTIFIED`, and the existing real A-Plus
      evidence/M4 artifact/communication artifact/validator-PASS state are
      all unchanged.

## Hard boundary (unchanged for any future M6.10 work)

No live query to RocketReach/ZoomInfo/Apollo/Hunter or any other
third-party contact-enrichment/verification/directory service; no gated
LinkedIn scraping; no LinkedIn message/connection request; no phone call;
no SMS; no email send; no A-Plus form submission; no expansion of the
A-Plus Path B pilot beyond its one authorized future recipient.
