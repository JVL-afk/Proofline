# M6.6B-5R — Tournament II human-review artifact recovery

**Status:** Complete; authorization consumed and review packages sealed

## Purpose

Generate new Sonnet samples for human-usefulness evaluation because the immutable original run
retained hashes but no reviewable output text. This recovery never claims to reproduce historical
outputs and cannot change the original seven disqualifications or five automated-safety results.

## Exact scope

- Original run: `3b37a366-3957-5f8b-94da-c83dac6f323e`.
- Original manifest: `f3695a274cdfbdba09a69d5143c15662057329c571f0dfda225819af9aa793ea`.
- Deployment: exact frozen `claude-sonnet-5` at `/v1/messages` with the frozen API version,
  task contracts, prompt hashes, schemas, thinking/effort settings, corpus, and evaluator.
- Tasks: evidence interpretation, contradiction analysis, opportunity reasoning, audit wording,
  and outreach wording.
- Cases: all 21 frozen case identities originally included in each surviving binding's package
  hash, for exactly 105 logical samples.
- Retry ceiling: one retry only after an unambiguous transient failure, at most 210 transport
  attempts.
- Pricing refreshed 2026-08-19 from Anthropic's official Sonnet 5 announcement: introductory
  USD 2/MTok input and USD 10/MTok output through 2026-08-31.
- Conservative primary ceiling: USD 2.376780; primary-plus-retry ceiling: USD 4.753560; separate
  hard cap: USD 5.00.

## Retention and review

ADR-0065 permits only normalized `HUMAN_REVIEW_RENDERED_OUTPUT` content required for paired review.
Raw transport bodies, headers, credentials, prompts, reasoning internals, and unrelated metadata
are not persisted. Each new sample must pass the frozen hard gates before inclusion.

Four unnamed reviewer slots receive independently seeded A/B ordering per case. Twenty slot-by-task
packages contain 420 blinded presentations over 105 unique samples. They remain sealed and cannot
be released until three distinct primary identities and one conditional adjudicator are frozen.
No route or canonical truth mutation is possible.

## Execution result

Recovery run `551c3c0d-d68e-568c-b9b8-14d947eec22e` completed all 105 logical calls with
105 transport attempts, zero retries, and zero new safety failures. Calculated cost was
USD 0.597460. All 105 samples produced usable normalized deterministic and candidate renderings.

The runner created 420 deterministic slot/case A/B assignments and 20 sealed slot-by-task packages.
Automated inspection found no provider name, model name, or machine telemetry in reviewer-facing
files. The four reviewer slots remain unnamed, package release remains prohibited, and no score was
created. The tracked safe summary records artifact hashes; rendered content remains ignored local
sealed data. The original Tournament II artifact remains byte-identical.
