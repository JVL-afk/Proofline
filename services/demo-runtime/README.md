# Demo Runtime

Separate-origin M4 simulation process. It renders only validated registered components, owns the
mandatory disclosure and CSP, and exchanges short-lived capabilities through a narrow injected
gateway. It has no database, core cookie, application/provider/research credential, external action
adapter, live AI, arbitrary code, publication, or public-sharing capability.
