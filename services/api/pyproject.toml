[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "opintel-api"
version = "0.7.0"
description = "Authenticated M0-M6 evidence, audit, outreach, and mock contact-control API"
requires-python = ">=3.13,<3.14"
dependencies = [
  "fastapi>=0.139,<0.140",
  "opintel-m0-core",
  "opintel-m0-local",
  "opintel-research-core",
  "opintel-research-local",
  "opintel-opportunity-core",
  "opintel-opportunity-local",
  "opintel-audit-core",
  "opintel-audit-local",
  "opintel-demo-core",
  "opintel-demo-local",
  "opintel-outreach-core",
  "opintel-outreach-local",
  "opintel-contact-core",
  "opintel-contact-local",
  "pydantic-settings>=2.10,<3",
  "uvicorn>=0.51,<0.52",
]

[project.scripts]
opintel-api = "opintel_api.main:run"

[tool.uv.sources]
opintel-m0-core = { workspace = true }
opintel-m0-local = { workspace = true }
opintel-research-core = { workspace = true }
opintel-research-local = { workspace = true }
opintel-opportunity-core = { workspace = true }
opintel-opportunity-local = { workspace = true }
opintel-audit-core = { workspace = true }
opintel-audit-local = { workspace = true }
opintel-demo-core = { workspace = true }
opintel-demo-local = { workspace = true }
opintel-outreach-core = { workspace = true }
opintel-outreach-local = { workspace = true }
opintel-contact-core = { workspace = true }
opintel-contact-local = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/opintel_api"]
